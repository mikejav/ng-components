import { CardRadioDirective } from './card-radio.directive';

describe('CardRadioDirective', () => {
  it('should create an instance', () => {
    const directive = new CardRadioDirective();
    expect(directive).toBeTruthy();
  });
});
